import { ACTION_TYPE } from "../Action";

const filterReducer=(state,action)=>{
    switch (action.type) {
        case "":
            
            break;
    
        default:
            return state
    }
}

export {filterReducer}